import { Box } from "@mui/material";

export default function ({ children }) {
    return (
        <Box sx={{ mx: '2.63rem', mt: '2.5rem', mb: '4.31rem' }}>
            { children }
        </Box>
    )
}